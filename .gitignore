/target
**/*.rs.bk
Cargo.lock

# Frontend (Tauri / Vite)
**/node_modules/
**/dist/
**/dist-ssr/
crates/bestel/ui/.vite/
crates/bestel/gen/
**/tsconfig.tsbuildinfo

# Tauri build artifacts
crates/bestel/target/

# Local secrets and logs
.env
.env.local
*.log

# Editor / OS
.idea/
.vscode/
*.swp
*.swo
.DS_Store
Thumbs.db

# Local scratch
tmp_*
*.tmp

# Cargo local config (contains machine-specific paths like protoc.exe)
.cargo/

# Battery run artifacts (per-machine, per-test outputs)
tests/eval/runs/

# bestel-driver scenario run artifacts (timestamped per-machine outputs)
tools/scenarios/runs/

# Design handoff bundles — ephemeral exploration artifacts from
# claude.ai/design. Extracted locally for reference, not versioned.
.design-bundle-v*/
.design-handoff/

# Internal sprint planning notes — detailed roadmaps + journals,
# kept out of the public repo to keep the root tidy for testers.
SPRINT_*.md

# Scratch test scenarios that reference user-local PoB build paths.
tools/scenarios/poedb-mods-tools-live.toml

# Generated dev-panel run exports (dev_export_all_runs IPC)
runs-export-*.json

# Battery scenario runtime output written by the dev panel's Live test tab
crates/bestel/ui/tools/

