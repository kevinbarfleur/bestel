# Generated from tests/eval/eval_set.toml — do not hand-edit.
# Category: mechanic

name = "mech_poe2_pinnacle_max_life"
provider = "any"
applies_to = ["poe2"]
prompt = """
what's the rough max hp pool of a poe2 pinnacle boss right now? i want to size my dps target
"""
timeout_secs = 240
cost = "low"

[[expectations]]
must_match = [
  "pob_calc",
  "Pinnacle",
  "0.5",
]
must_not_match = [
  "Eater of Worlds",
  "Sirus",
]
must_not_expose_reasoning = true
must_have_panel_data_first = true
must_cite_only_fetched_urls = true
