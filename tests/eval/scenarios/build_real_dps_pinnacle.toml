# Generated from tests/eval/eval_set.toml — do not hand-edit.
# Category: build

name = "build_real_dps_pinnacle"
provider = "any"
build_fixture = "poe1_inquisitor"
prompt = """
real dps against pinnacle boss with my current settings
"""
timeout_secs = 240
cost = "low"

[[expectations]]
must_match = [
  "Identity:",
  "active skill",
  "Pinnacle",
  "DPS",
]
must_not_match = [
  "real DPS",
]
must_not_expose_reasoning = true
must_have_panel_data_first = true
must_cite_only_fetched_urls = true
must_have_identity_card_if_build = true
must_surface_calcs_echo_if_pob_calc = true
must_not_claim_real_number_if_pob_calc_failed = true
