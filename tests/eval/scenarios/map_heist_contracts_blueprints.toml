# Generated from tests/eval/eval_set.toml — do not hand-edit.
# Category: mapping

name = "map_heist_contracts_blueprints"
provider = "any"
prompt = """
fresh league - should i farm heist contracts first or jump straight to blueprints
"""
timeout_secs = 240
cost = "low"

[[expectations]]
must_match = [
  "contract",
  "blueprint",
]
must_not_expose_reasoning = true
must_have_panel_data_first = true
must_cite_only_fetched_urls = true
