# Generated from tests/eval/eval_set.toml — do not hand-edit.
# Category: mechanic

name = "mech_rt_ailments"
provider = "any"
prompt = """
can i still ignite with RT or no
"""
timeout_secs = 240
cost = "low"

[[expectations]]
must_match = [
  "chance to ignite",
  "Resolute Technique",
  "non-crit",
]
must_not_match = [
  "no, RT removes ignite",
  "RT prevents ignite entirely",
]
must_not_expose_reasoning = true
must_have_panel_data_first = true
must_cite_only_fetched_urls = true
