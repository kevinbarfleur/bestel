# Generated from tests/eval/eval_set.toml — do not hand-edit.
# Category: mapping

name = "map_breach_layout"
provider = "any"
prompt = """
best map layout for breach farming right now
"""
timeout_secs = 240
cost = "low"

[[expectations]]
must_match = [
  "layout",
  "breach",
]
must_not_expose_reasoning = true
must_have_panel_data_first = true
must_cite_only_fetched_urls = true
