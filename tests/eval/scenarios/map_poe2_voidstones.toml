# Generated from tests/eval/eval_set.toml — do not hand-edit.
# Category: mapping

name = "map_poe2_voidstones"
provider = "any"
applies_to = ["poe2"]
prompt = """
do voidstones still gate atlas progression in poe2 or did 0.5 change that
"""
timeout_secs = 240
cost = "low"

[[expectations]]
must_match = [
  "PoE2",
  "0.5",
  "atlas",
]
must_not_match = [
  "fandom",
]
must_not_expose_reasoning = true
must_have_panel_data_first = true
must_cite_only_fetched_urls = true
