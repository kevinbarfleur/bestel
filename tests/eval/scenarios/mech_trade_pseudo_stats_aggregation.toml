# Generated from tests/eval/eval_set.toml — do not hand-edit.
# Category: mechanic

name = "mech_trade_pseudo_stats_aggregation"
provider = "any"
prompt = """
how do the pseudo stats actually work on the trade site? do they search across my whole gear or just one item
"""
timeout_secs = 240
cost = "low"

[[expectations]]
must_match = [
  "item",
  "single item",
  "listed item",
]
must_not_match = [
  "whole build",
  "all gear",
  "every slot",
]
must_not_expose_reasoning = true
must_have_panel_data_first = true
must_cite_only_fetched_urls = true
