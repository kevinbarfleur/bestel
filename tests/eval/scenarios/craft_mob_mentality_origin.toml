# Generated from tests/eval/eval_set.toml — do not hand-edit.
# Category: craft

name = "craft_mob_mentality_origin"
provider = "any"
prompt = """
where does the Mob Mentality cluster jewel notable come from? im trying to roll one
"""
timeout_secs = 240
cost = "low"

[[expectations]]
must_match = [
  "Minion Damage",
  "cluster",
  "notable",
]
must_not_match = [
  "amulet",
  "ring",
]
must_not_expose_reasoning = true
must_have_panel_data_first = true
must_cite_only_fetched_urls = true
