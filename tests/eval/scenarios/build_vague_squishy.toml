# Generated from tests/eval/eval_set.toml — do not hand-edit.
# Category: build

name = "build_vague_squishy"
provider = "any"
prompt = """
my build feels like ass, what am i doing wrong
"""
timeout_secs = 240
cost = "low"

[[expectations]]
must_match = [
  "which",
  "ask",
]
must_not_match = [
  "save your build in PoB",
  "you should swap",
]
must_not_expose_reasoning = true
must_have_panel_data_first = true
must_cite_only_fetched_urls = true
