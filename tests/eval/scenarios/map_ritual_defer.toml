# Generated from tests/eval/eval_set.toml — do not hand-edit.
# Category: mapping

name = "map_ritual_defer"
provider = "any"
prompt = """
how does deferring a ritual work mechanically and when should i use it
"""
timeout_secs = 240
cost = "low"

[[expectations]]
must_match = [
  "defer",
  "ritual",
]
must_not_expose_reasoning = true
must_have_panel_data_first = true
must_cite_only_fetched_urls = true
