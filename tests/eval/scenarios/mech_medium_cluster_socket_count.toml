# Generated from tests/eval/eval_set.toml — do not hand-edit.
# Category: mechanic

name = "mech_medium_cluster_socket_count"
provider = "any"
prompt = """
if i have a Medium Cluster Jewel, how many Small Cluster Jewel sockets does it actually create
"""
timeout_secs = 240
cost = "low"

[[expectations]]
must_match = [
  "one",
  "1 small",
  "single small",
]
must_not_match = [
  "two",
  "three",
]
must_not_expose_reasoning = true
must_have_panel_data_first = true
must_cite_only_fetched_urls = true
