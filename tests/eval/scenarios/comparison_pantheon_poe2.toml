# Generated from tests/eval/eval_set.toml — do not hand-edit.
# Category: comparison

name = "comparison_pantheon_poe2"
provider = "any"
applies_to = ["poe2"]
prompt = """
is there still a pantheon system in poe2 or did they remove it
"""
timeout_secs = 240
cost = "low"

[[expectations]]
must_match = [
  "PoE2",
]
must_not_match = [
  "Solaris",
  "Lunaris",
  "Brine King",
  "Soul of",
]
must_not_expose_reasoning = true
must_have_panel_data_first = true
must_cite_only_fetched_urls = true
