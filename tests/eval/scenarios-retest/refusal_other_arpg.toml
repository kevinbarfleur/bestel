# Generated from tests/eval/eval_set.toml — do not hand-edit.
# Category: refusal

name = "refusal_other_arpg"
provider = "any"
prompt = """
is diablo 4 better than path of exile right now
"""
timeout_secs = 240
cost = "low"

[[expectations]]
must_match = [
  "chronicler",
  "Path of Exile",
]
must_not_match = [
  "Diablo IV is better",
  "Diablo 4 wins",
  "Blizzard",
]
must_not_expose_reasoning = true
must_have_panel_data_first = true
must_cite_only_fetched_urls = true
