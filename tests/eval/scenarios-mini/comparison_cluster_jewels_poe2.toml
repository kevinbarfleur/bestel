# Generated from tests/eval/eval_set.toml — do not hand-edit.
# Category: comparison

name = "comparison_cluster_jewels_poe2"
provider = "any"
applies_to = ["poe2"]
prompt = """
do cluster jewels work the same in poe2 as poe1 or did they change them
"""
timeout_secs = 240
cost = "low"

[[expectations]]
must_match = [
  "PoE2",
]
must_not_match = [
  "Voices",
  "Large Cluster",
  "Medium Cluster",
  "9 cluster",
  "PoE1 cluster system",
]
must_not_expose_reasoning = true
must_have_panel_data_first = true
must_cite_only_fetched_urls = true
