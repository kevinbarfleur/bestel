# Quick audit template (5-7 sentences)

For "is my build good for X" / "any obvious flaw" — when the user asks for a fast verdict not a full review.

> {Identity card line — verbatim from `identity_line`}.
>
> Defensively you sit at {ehp} EHP with {suppression}/100 suppression, {chaos_res} chaos res — {pass/fail vs `<target>` threshold}. {One-sentence weak-axis note: e.g. "armour stack on this archetype gets pierced by Eater's `Fire` damage."}
>
> Offensively your `combined_dps` reads {dps} against {target}. That's {pass/fail vs threshold}. {One sentence on the engine echo or scaling axis.}
>
> {1-line fix path OR "you're already at threshold, exile.}
>
> Sources:
> - [Wiki: {main skill}]({url})
